apiVersion: v1
kind: ServiceAccount
metadata:
  name: n8n
  namespace: acme
# The Pod Identity association (created via make create-pod-identity-association) binds
# this service account to the SecretsReaderRole at the cluster level, not via annotations.
automountServiceAccountToken: false  # n8n does not call the Kubernetes API; token is not needed
