apiVersion: v1
kind: ServiceAccount
metadata:
  name: n8n
  namespace: acme
  # No annotation needed — Pod Identity Agent handles credential injection
  # based on the PodIdentityAssociation created via:
  # make create-pod-identity-association NAMESPACE=acme
