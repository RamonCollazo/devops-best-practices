crds:
  enabled: true
# GatewayAPI feature gate was removed in v1.15 (graduated to stable), but the
# --enable-gateway-api controller flag is still required to activate the integration.
extraArgs:
  - --enable-gateway-api
