apiVersion: v1
kind: Namespace
metadata:
  name: shared-gateway
  labels:
    # Required so HTTPRoutes in this namespace (e.g. cert-manager HTTP01 solver)
    # can attach to the shared Gateway, which uses a namespace Selector.
    shared-gateway: "true"
